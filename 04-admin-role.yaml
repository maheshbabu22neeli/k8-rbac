apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: roboshop
  name: admin-role

rules:
  - apiGroups: ["*"]         # * indicates all group
    resources: ["*"]
    verbs: ["*"]
